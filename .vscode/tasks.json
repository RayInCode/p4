{
    "tasks": [
        {
            "type": "shell",
            "label": "compile for debug",
            "command": "make all",
            "options": {
                "cwd": "${fileDirname}"
            }
        }
    ],
    "version": "2.0.0"
}